services:
  - type: web
    name: collabai-frontend
    env: node
    rootDir: frontend
    buildCommand: npm install
    startCommand: npm run build && npx next start -p $PORT
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: PORT
        value: 3000
